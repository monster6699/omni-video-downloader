# 复制为 .env 后 docker compose 会自动加载（在 docker/ 目录执行 compose）
# cp .env.example .env

MYSQL_ROOT_PASSWORD=rootpass
MYSQL_PASSWORD=omni123
MYSQL_PORT=3306
REDIS_PORT=6379
HTTP_PORT=80
BACKEND_PORT=8000

# 必填：AI 功能
OPENAI_API_KEY=

# 生产务必修改
JWT_SECRET=change-me-to-a-long-random-string

# 可选
# OPENAI_API_BASE=https://api.deepseek.com
# OPENAI_MODEL=deepseek-chat
# TRANSLATE_MAX_OUTPUT_TOKENS=8192
# TASK_MODE=local
# CORS_ORIGINS=["http://localhost","http://你的域名"]
