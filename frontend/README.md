# OmniVideo 前端

Vue 3 + TypeScript + Vite + Vue Router + Naive UI + TailwindCSS。

## 开发

```bash
npm install
npm run dev
```

默认通过 Vite 代理将 `/api` 转发到后端（见 `vite.config`）。

## 项目文档

见仓库根目录 [**docs/README.md**](../docs/README.md)（含 **产品与运维说明**、**项目现状** 等）。
